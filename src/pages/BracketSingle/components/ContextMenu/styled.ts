import styled from 'styled-components';

const OverflayStyled = styled.div`
	position: fixed;
	width: 100%;
	height: 100%;
	z-index: 1000;
	left: 0;
	top: -10px;
	background: rgb(0 0 0 / 10%);
`;

export default {
	OverflayStyled,
};
